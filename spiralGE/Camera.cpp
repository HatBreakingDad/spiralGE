#include "Camera.h"


namespace sge {

	Camera::Camera(const float minRendDist, const float maxRendDist, const unsigned int scrWidth, const unsigned int scrHeight) 
//		: m_perspTransform(glm::perspective(fov, scrWidth / scrHeight, minRendDist, maxRendDist))

	{

	}
	Camera::~Camera()
	{
	}
}

